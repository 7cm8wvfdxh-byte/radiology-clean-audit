# Backend calistirmadan once bu dosyayi .env olarak kopyalayin:
# cp .env.example .env

# !! ONEMLI: Asagidaki CHANGE_ME degerlerini MUTLAKA degistirin !!
# Zayif secret'lar production ortaminda guvenlik acigina neden olur.
# Guvenli anahtar uretmek icin: python -c "import secrets; print(secrets.token_hex(32))"

# Audit imzasi icin gizli anahtar (en az 32 karakter, rastgele)
AUDIT_SECRET=CHANGE_ME_STRONG_SECRET_HERE

# JWT token icin gizli anahtar (en az 32 karakter, rastgele)
JWT_SECRET=CHANGE_ME_STRONG_JWT_SECRET_HERE

# Token gecerlilik suresi (dakika)
ACCESS_TOKEN_EXPIRE_MINUTES=480

# Frontend'den izin verilen originler (virgul ile ayrilmis)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Verify URL'inin base kismi (QR kod ve audit pack'e yazilir)
VERIFY_BASE_URL=http://localhost:8000

# Ilk admin kullanicisi (ilk calistirmada otomatik olusturulur)
DEFAULT_ADMIN_USER=admin
DEFAULT_ADMIN_PASS=CHANGE_ME_ADMIN_PASSWORD

# Claude API key (radyolog ajan icin gerekli)
# ANTHROPIC_API_KEY=sk-ant-...

# Veritabani URL (varsayilan: SQLite, production icin PostgreSQL onerilir)
# DATABASE_URL=sqlite:///./radiology_clean.db
# DATABASE_URL=postgresql://user:pass@localhost:5432/radiology
